---
const navigationList = [
	{ to: "/", name: "home" },
	{ to: "/work", name: "work" },
	{ to: "/blog", name: "blog" },
	{ to: "/contact", name: "contact" },
	{ to: "/ceda", name: "ceda" },
]

const pathname = Astro.url.pathname
---

<header
	id="nav-header"
	class="flex gap-4 mb-20 opacity-0">
	{
		navigationList.map((nl) => (
			<a
				href={nl.to}
				class="after:transition-[width] after:w-0 after:h-[1px] after:block after:bg-white hover:after:w-full"
				aria-current={nl.to === pathname ? "page" : null}>
				{nl.name}
			</a>
		))
	}
</header>

<script is:inline>
	const header = document.getElementById("nav-header")
	const hasAnimated = sessionStorage.getItem("headerAnimated")
	if (!hasAnimated) {
		header.classList.add("animate-blur-reveal")
		sessionStorage.setItem("headerAnimated", "true")
	} else {
		header.classList.remove("opacity-0")
	}

	if (location.pathname.startsWith("/blog")) {
		header.querySelector("[href='/blog']").classList.add("after:w-full")
	} else {
		header.querySelector("[aria-current=page]")?.classList.add("after:w-full")
	}
</script>

import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class="fixed z-10 w-full bottom-0 text-center py-3 bg-yellow-200">
      <p class="dark:text-black">🚧 Work in progress 🚧</p>
    </div>
  );
});
